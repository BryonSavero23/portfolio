# Copy to .env and fill in from your Supabase project settings (Project Settings → API).
# Both values are safe to expose in the browser bundle; row-level security is what
# protects message data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
